Drop real wallpaper image files here, organized by category (e.g. `mountains/mountains-01.jpg`).

Then in `js/data.js`, replace the generated `bg` gradient for each wallpaper with a path to the real file, e.g.:

```js
bg: "url('assets/wallpapers/mountains/mountains-01.jpg')"
```
